/// <reference types="react" />
export interface HeartProp {
    isClick: boolean;
    styles?: any;
    onClick: () => void;
}
export default function Heart({ isClick, onClick, styles }: HeartProp): JSX.Element;
